{
	"name": "cv_page",
	"version": "1.0.0",
	"description": "Projects' portfolio",
	"main": "index.js",
	"scripts": {
		"dev": "dead-server --port=3000 --host=localhost --port=4607",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Inga Sinkeviciute",
	"license": "ISC",
	"devDependencies": {
		"dead-server": "^1.0.9"
	}
}
